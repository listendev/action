name: codeql

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '55 14 * * 1'

jobs:
  call-codeql-analysis:
    name: analysis
    uses: actions/reusable-workflows/.github/workflows/codeql-analysis.yml@main
